########################################################################################
# This corresponds to the infrastructure configuration file.
# It contains every parameters used to set up infrastructure.
#
# This file is the most updated configuration file and can be used as a default one.
########################################################################################

data_warehouse:
  connect: True

# Database connection information
db:
  # Host name (server containing database)
  host: demand_forecast_data_warehouse_1
  port: 5432
  user: gamma
  password: example
  database: data_warehouse
